import React from "react"

import styled from "styled-components"

const AlbumToggleButton = styled.button`
  outline: none;
  cursor: pointer;
  border: none;
  color: white;
`

export default AlbumToggleButton
